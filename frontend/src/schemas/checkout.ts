/**
 * Reexport del contrato de checkout — definido en `gateway.ts`.
 */
export {
  checkoutRequestSchema,
  parseCheckoutRequest,
  type CheckoutRequest,
  type CheckoutRequestInput,
} from './gateway'
